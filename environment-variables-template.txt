# Firebase Configuration (Required)
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

# Environment Settings
VITE_APP_ENV=development # development, staging, or production
VITE_APP_NAME=Agency CRM
VITE_APP_URL=http://localhost:3000

# Feature Flags (Optional - defaults to false if not set)
VITE_ENABLE_OFFLINE_MODE=true
VITE_ENABLE_FILE_UPLOAD=true
VITE_ENABLE_NOTIFICATIONS=false # Set to true when implementing notifications

# Firebase Performance Settings (Optional)
VITE_FIRESTORE_CACHE_SIZE_MB=50
VITE_STORAGE_MAX_UPLOAD_SIZE_MB=10
VITE_STORAGE_ALLOWED_TYPES=image/jpeg,image/png,application/pdf,application/msword

# Security Settings (Optional)
VITE_MAX_FILE_SIZE_MB=10
VITE_MAX_FILES_PER_UPLOAD=5
VITE_ALLOWED_FILE_TYPES=.jpg,.jpeg,.png,.pdf,.doc,.docx

# Rate Limiting (Optional)
VITE_API_RATE_LIMIT=100 # requests per minute
VITE_API_RATE_LIMIT_WINDOW=60000 # milliseconds

# Cache Settings (Optional)
VITE_CACHE_TTL=3600 # seconds
VITE_STATIC_CACHE_VERSION=1

# Instructions:
# 1. Rename this file to .env.local for local development
# 2. Fill in the Firebase configuration values from your Firebase project
# 3. Adjust other values as needed
# 4. Never commit .env.local to version control

# Note: All variables must be prefixed with VITE_ to be accessible in the frontend code
# Values provided here are examples/defaults - adjust according to your needs
